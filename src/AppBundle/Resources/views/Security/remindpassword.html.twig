{% extends 'AppBundle::layout.html.twig' %}

{% block head_title %}Восстановление доступа{% endblock %}
    {% block head_additions %}
        <script src='https://www.google.com/recaptcha/api.js'></script>
    {% endblock %}

{% block body_content %}
    <div class="signin center-block">
        <h2 class="form-signin-heading">Восстановление доступа</h2>
        {% set form = forms.profile %}
        {{ form_start(form) }}
        {% set _errors = form.email.vars.errors %}
        {% set _class = 'form-group' ~ (_errors|length > 0 ? ' has-error' : '') %}
        <div class="{{ _class }}">
            {{ form_label(form.email, null, {'label_attr': {'class': 'control-label'}}) }} <span class="required">*</span>
            {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
            {% if _errors|length > 0 %}{% for _error in _errors %}<span class="help-block">{{ _error.message|e }}</span>{% endfor %}{% endif %}
        </div>
        {% set error = form.vars.errors|length > 0 %}
        {% if error %}{% for error in form.vars.errors %}<span class="text-danger help-block" style="color:#a94442;">{{ error.message }}</span>{% endfor %}{% endif %}
        <div class="g-recaptcha" data-sitekey="6LeQfCgUAAAAANpy_1AId28ubn-55uRdjbcZYxvs"></div>
        <hr>
        <p>Знаком <span class="required">*</span> помечены поля, обязательные для заполнения.</p>
        <hr>
        <button type="submit" class="btn btn-success">
            <span class="glyphicon glyphicon-save" aria-hidden="true"></span> Восстановить доступ
        </button>
        {{ form_end(form) }}
    </div>
{% endblock %}




